function mapStateToProps(state){
	Return {
		appState: state
	};
}

then // this.props.appState


-------------------

function mapStateToProps(state){
	Return {
		users: state.users
	};
}
